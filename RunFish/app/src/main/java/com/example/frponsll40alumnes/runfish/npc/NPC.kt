package com.example.frponsll40alumnes.runfish.npc

import com.example.frponsll40alumnes.runfish.Position

abstract class NPC(var value : Int, override var x: Int = 0, override var y: Int = 0) :
    Position {
}